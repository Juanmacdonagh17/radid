radid is a simple python app that gives you random id's (uniprot, pdb, etc) at will.
it's very usefull if you need to test pipelines or just want to check data! 

clone and run ./radid.py -help for more information
